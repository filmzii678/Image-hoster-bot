{
  "name": "image-hoster-bot",
  "version": "1.0.0",
  "main": "_worker.js",
  "scripts": {
    "build": "echo 'Production build complete' && exit 0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "hono": "^4.0.0"
  }
}
