{
  "name": "image-hoster-bot",
  "version": "1.0.0",
  "main": "_worker.js",
  "scripts": {
    "build": "npm install && echo 'Dependencies installed'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "hono": "^4.0.0"
  }
}
